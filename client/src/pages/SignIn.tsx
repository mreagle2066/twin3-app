import { Button } from "@/components/ui/button";
import { startLogin } from "@/const";
import { ArrowLeft, ArrowRight, LockKeyhole, ShieldCheck, X } from "lucide-react";
import { Link } from "wouter";

export default function SignIn() {
  return <main className="grid-surface flex min-h-screen items-center justify-center bg-[#0b0d1a] px-5 text-slate-50"><div className="w-full max-w-md rounded-[1.7rem] border border-white/[.09] bg-[#14172b]/95 p-7 shadow-[0_32px_100px_rgba(0,0,0,.45)] backdrop-blur-xl sm:p-9"><Link href="/" className="inline-flex items-center gap-2 text-sm text-slate-400 transition-colors hover:text-white"><ArrowLeft className="size-4" />Back to twin3.ai</Link><div className="mt-9 flex size-11 items-center justify-center rounded-2xl bg-gradient-to-br from-cyan-200 to-indigo-400 text-slate-950"><X className="size-5" strokeWidth={3} /></div><p className="eyebrow mt-7">Secure workspace access</p><h1 className="mt-3 font-[Manrope] text-3xl font-bold tracking-[-.05em] text-white">Enter Twin3</h1><p className="mt-3 text-sm leading-6 text-slate-400">Sign in with your Twin3 workspace account or use X to create a secure account and connect the profile your agents will operate for.</p><div className="mt-8 space-y-3"><Button onClick={() => window.location.assign("/api/x/authorize?intent=signin")} className="button-press h-12 w-full rounded-xl bg-white text-slate-950 hover:bg-slate-100"><X className="mr-2 size-4" strokeWidth={2.5} />Continue with X <ArrowRight className="ml-2 size-4" /></Button><Button onClick={startLogin} variant="outline" className="button-press h-12 w-full rounded-xl border-white/10 bg-white/[.035] text-slate-100 hover:bg-white/[.07]">Continue with workspace sign-in</Button></div><div className="mt-7 rounded-xl border border-emerald-300/10 bg-emerald-300/[.04] p-4"><div className="flex items-center gap-2 text-xs font-semibold text-emerald-100"><ShieldCheck className="size-4" />Approval-first X access</div><p className="mt-2 text-xs leading-5 text-slate-500">Twin3 requests only the permissions needed to connect your account. Reply and content drafts remain in Draft Mode until you approve them.</p></div><p className="mt-6 flex items-center gap-2 text-[11px] text-slate-600"><LockKeyhole className="size-3.5" />Your X connection can be revoked anytime in Safety &amp; Controls.</p></div></main>;
}
