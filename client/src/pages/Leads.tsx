import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { Empty } from "@/pages/Dashboard";
import { formatNumber } from "@/lib/growth";
import { Filter, Loader2, Plus, Search, Sparkles, Star, UsersRound } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

const tagOptions = ["all", "potential user", "partner", "investor", "KOL"] as const;

export default function Leads() {
  const utils = trpc.useUtils(); const [search, setSearch] = useState(""); const [tag, setTag] = useState<(typeof tagOptions)[number]>("all"); const [minScore, setMinScore] = useState(0); const [showAdd, setShowAdd] = useState(false); const [form, setForm] = useState({ name: "", handle: "", category: "", audienceSize: 0, relevance: 50, interestLevel: "medium" as "low" | "medium" | "high", leadScore: 50, opportunityTag: "potential user" as "potential user" | "partner" | "investor" | "KOL" });
  const leads = trpc.growth.leads.list.useQuery({ search, opportunityTag: tag === "all" ? undefined : tag, minScore }); const create = trpc.growth.leads.create.useMutation({ onSuccess: () => { utils.growth.leads.list.invalidate(); setShowAdd(false); toast.success("Lead added to intelligence workspace"); }, onError: (error) => toast.error(error.message) });
  const saveLead = (event: React.FormEvent) => { event.preventDefault(); create.mutate(form); };
  return <div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><p className="eyebrow">AI-powered CRM</p><h1 className="mt-2 font-[Manrope] text-3xl font-bold tracking-[-.045em] text-white">Lead intelligence</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">Score relevance, track interest, and understand which contacts represent a potential user, partner, investor, or KOL opportunity.</p></div><Button onClick={() => setShowAdd(!showAdd)} className="button-press rounded-xl bg-indigo-300 text-slate-950 hover:bg-indigo-200"><Plus className="mr-2 size-4" />Add lead</Button></div>{showAdd && <form onSubmit={saveLead} className="glass-card mt-6 rounded-2xl p-6"><div className="flex items-center gap-3"><div className="flex size-8 items-center justify-center rounded-lg bg-indigo-400/10 text-indigo-200"><Plus className="size-4" /></div><div><h2 className="text-sm font-semibold text-white">Create an intelligence profile</h2><p className="mt-0.5 text-xs text-slate-500">Lead score is always kept on a 0–100 scale.</p></div></div><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Name" className="border-white/10 bg-white/[.035]" /><Input required value={form.handle} onChange={(e) => setForm({ ...form, handle: e.target.value })} placeholder="@handle" className="border-white/10 bg-white/[.035]" /><Input required value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} placeholder="Category" className="border-white/10 bg-white/[.035]" /><select value={form.opportunityTag} onChange={(e) => setForm({ ...form, opportunityTag: e.target.value as typeof form.opportunityTag })} className="h-10 rounded-lg border border-white/10 bg-white/[.035] px-3 text-xs text-slate-300"><option>potential user</option><option>partner</option><option>investor</option><option>KOL</option></select><Input type="number" min={0} value={form.audienceSize} onChange={(e) => setForm({ ...form, audienceSize: Number(e.target.value) })} placeholder="Audience size" className="border-white/10 bg-white/[.035]" /><Input type="number" min={0} max={100} value={form.relevance} onChange={(e) => setForm({ ...form, relevance: Number(e.target.value) })} placeholder="Relevance" className="border-white/10 bg-white/[.035]" /><Input type="number" min={0} max={100} value={form.leadScore} onChange={(e) => setForm({ ...form, leadScore: Number(e.target.value) })} placeholder="Lead score" className="border-white/10 bg-white/[.035]" /><select value={form.interestLevel} onChange={(e) => setForm({ ...form, interestLevel: e.target.value as typeof form.interestLevel })} className="h-10 rounded-lg border border-white/10 bg-white/[.035] px-3 text-xs text-slate-300"><option value="low">Low interest</option><option value="medium">Medium interest</option><option value="high">High interest</option></select></div><div className="mt-5 flex justify-end gap-3"><Button type="button" onClick={() => setShowAdd(false)} variant="outline" className="border-white/10 bg-white/[.03] text-slate-400 hover:bg-white/[.06] hover:text-slate-200">Cancel</Button><Button disabled={create.isPending} className="button-press rounded-xl bg-indigo-300 text-slate-950 hover:bg-indigo-200">{create.isPending && <Loader2 className="mr-2 size-4 animate-spin" />}Save lead</Button></div></form>}<section className="mt-8 overflow-hidden rounded-2xl border border-white/[.08] bg-[#17192d]"><div className="flex flex-col gap-4 border-b border-white/[.07] p-5 xl:flex-row xl:items-center xl:justify-between"><div className="relative max-w-sm flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-600" /><Input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search name, handle, category…" className="border-white/10 bg-white/[.035] pl-9" /></div><div className="flex flex-wrap items-center gap-3"><div className="flex items-center gap-2 text-xs text-slate-500"><Filter className="size-3.5" />Filters</div><select value={tag} onChange={(e) => setTag(e.target.value as typeof tag)} className="h-9 rounded-lg border border-white/10 bg-white/[.035] px-3 text-xs text-slate-300"><option value="all">All opportunity tags</option><option value="potential user">potential user</option><option value="partner">partner</option><option value="investor">investor</option><option value="KOL">KOL</option></select><select value={minScore} onChange={(e) => setMinScore(Number(e.target.value))} className="h-9 rounded-lg border border-white/10 bg-white/[.035] px-3 text-xs text-slate-300"><option value={0}>Any score</option><option value={50}>50+ score</option><option value={70}>70+ score</option><option value={85}>85+ score</option></select></div></div>{leads.data?.length ? <div className="overflow-x-auto"><table className="w-full min-w-[940px] text-left"><thead className="border-b border-white/[.06] bg-white/[.018]"><tr>{["Lead", "Category", "Audience", "Relevance", "Interest", "Opportunity", "Lead score"].map((head) => <th key={head} className="px-6 py-3.5 text-[10px] font-semibold uppercase tracking-[.14em] text-slate-500">{head}</th>)}</tr></thead><tbody>{leads.data.map((lead) => <tr key={lead.id} className="border-b border-white/[.05] last:border-0"><td className="px-6 py-4"><div className="flex items-center gap-3"><div className="flex size-8 items-center justify-center rounded-lg bg-white/[.05] text-slate-400"><UsersRound className="size-3.5" /></div><div><p className="text-sm font-medium text-slate-200">{lead.name}</p><p className="mono mt-0.5 text-[10px] text-slate-600">{lead.handle}</p></div></div></td><td className="px-6 py-4 text-xs text-slate-400">{lead.category}</td><td className="px-6 py-4 mono text-xs text-slate-400">{formatNumber(lead.audienceSize)}</td><td className="px-6 py-4 text-xs text-slate-300">{lead.relevance}/100</td><td className="px-6 py-4"><span className={`text-xs ${lead.interestLevel === "high" ? "text-emerald-200" : lead.interestLevel === "low" ? "text-slate-500" : "text-amber-200"}`}>{lead.interestLevel}</span></td><td className="px-6 py-4">{lead.opportunityTag && <Badge className="border border-indigo-300/15 bg-indigo-300/[.06] px-2 py-0.5 text-[9px] text-indigo-100 hover:bg-indigo-300/[.06]">{lead.opportunityTag}</Badge>}</td><td className="px-6 py-4"><div className="flex items-center gap-2"><span className="mono text-sm font-medium text-white">{lead.leadScore}</span><div className="h-1.5 w-16 overflow-hidden rounded-full bg-white/[.08]"><div className="h-full rounded-full bg-gradient-to-r from-cyan-300 to-indigo-400" style={{ width: `${lead.leadScore}%` }} /></div></div></td></tr>)}</tbody></table></div> : <Empty label="No leads match your filters" detail="Add a lead profile or adjust the current intelligence filters to broaden the view." />}</section><div className="mt-6 flex items-center gap-3 rounded-xl border border-indigo-300/10 bg-indigo-300/[.035] px-5 py-4"><Sparkles className="size-4 shrink-0 text-indigo-200" /><p className="text-xs leading-5 text-slate-500">Automatic opportunity tagging uses the fixed vocabulary <span className="text-slate-300">potential user, partner, investor, KOL</span> to make prioritization consistent.</p></div></div>;
}
