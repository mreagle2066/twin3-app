import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { Check, ChevronRight, FileUp, Filter, Loader2, MessageSquareText, Play, ScanSearch, Send, Sparkles, UsersRound } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

const sourceOptions = [{ icon: UsersRound, label: "X followers", detail: "Build from a relevant audience" }, { icon: ScanSearch, label: "Competitor followers", detail: "Discover adjacent communities" }, { icon: MessageSquareText, label: "Keywords", detail: "Surface active conversations" }, { icon: FileUp, label: "CSV import", detail: "Bring your own lead list" }];
const steps = ["List", "Filters", "Message", "AI variations", "Preview", "Schedule", "Launch"];

export default function Outreach() {
  const utils = trpc.useUtils();
  const [source, setSource] = useState("X followers"); const [audience, setAudience] = useState(""); const [message, setMessage] = useState(""); const [name, setName] = useState(""); const [dailyLimit, setDailyLimit] = useState(30); const [variations, setVariations] = useState<string[]>([]);
  const generate = trpc.growth.ai.generateVariations.useMutation({ onSuccess: (data) => setVariations(data.variations), onError: (error) => toast.error(error.message) });
  const create = trpc.growth.campaigns.create.useMutation({ onSuccess: () => { utils.growth.dashboard.invalidate(); utils.growth.campaigns.list.invalidate(); toast.success("Campaign saved as a draft"); setName(""); }, onError: (error) => toast.error(error.message) });
  const submit = (event: React.FormEvent) => { event.preventDefault(); create.mutate({ name, targetAudience: audience, dailyLimit, status: "draft" }); };
  return <div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><p className="eyebrow">Agent 01 · Find → Filter → Contact</p><h1 className="mt-2 font-[Manrope] text-3xl font-bold tracking-[-.045em] text-white">Outreach Agent</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Build audience lists with intent, set a message your team can stand behind, and create natural variations before launch.</p></div><div className="rounded-xl border border-[#e8e1c5]/15 bg-[#e8e1c5]/[.05] px-4 py-2.5 text-xs text-[#d9ecd4]"><span className="mr-2 inline-block size-1.5 rounded-full bg-[#e8e1c5]" />Relevance-first campaign controls</div></div><div className="mt-8 overflow-x-auto"><div className="flex min-w-[650px] items-center gap-0">{steps.map((step, index) => <div key={step} className="flex items-center"><div className={`flex items-center gap-2 rounded-full px-3 py-2 ${index === 0 ? "bg-[#e8e1c5] text-[#20362b]" : "text-slate-500"}`}><span className="mono text-[10px]">0{index + 1}</span><span className="text-xs font-medium">{step}</span></div>{index < steps.length - 1 && <ChevronRight className="mx-1 size-3 text-slate-700" />}</div>)}</div></div><form onSubmit={submit} className="mt-8 grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><section className="space-y-5"><div className="glass-card rounded-2xl p-6"><div className="flex items-center gap-3"><div className="flex size-8 items-center justify-center rounded-lg bg-[#e8e1c5]/10 text-[#e8e1c5]"><UsersRound className="size-4" /></div><div><h2 className="text-sm font-semibold text-white">1. Choose a lead source</h2><p className="mt-0.5 text-xs text-slate-500">Select the signal you want the agent to explore.</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{sourceOptions.map((option) => <button key={option.label} type="button" onClick={() => setSource(option.label)} className={`rounded-xl border p-4 text-left transition-colors ${source === option.label ? "border-[#e8e1c5]/40 bg-[#e8e1c5]/[.08]" : "border-white/[.08] bg-white/[.02] hover:bg-white/[.05]"}`}><div className="flex items-center justify-between"><option.icon className={`size-4 ${source === option.label ? "text-[#e8e1c5]" : "text-slate-500"}`} />{source === option.label && <Check className="size-4 text-[#e8e1c5]" />}</div><p className="mt-5 text-sm font-medium text-slate-200">{option.label}</p><p className="mt-1 text-xs text-slate-500">{option.detail}</p></button>)}</div></div><div className="glass-card rounded-2xl p-6"><div className="flex items-center gap-3"><div className="flex size-8 items-center justify-center rounded-lg bg-[#9fbd9d]/10 text-[#d9ecd4]"><Filter className="size-4" /></div><div><h2 className="text-sm font-semibold text-white">2. Configure targeting</h2><p className="mt-0.5 text-xs text-slate-500">Use filters to create a list worth contacting.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Target audience" hint="Describe the ICP"><Input required value={audience} onChange={(event) => setAudience(event.target.value)} placeholder="e.g. AI founders building in Web3" className="border-white/10 bg-white/[.035]" /></Field><Field label="Location" hint="Optional"><Input placeholder="Global or a target region" className="border-white/10 bg-white/[.035]" /></Field><Field label="Bio keywords" hint="Optional"><Input placeholder="AI, agents, Web3" className="border-white/10 bg-white/[.035]" /></Field><Field label="Follower range" hint="Optional"><Input placeholder="1,000 – 50,000" className="border-white/10 bg-white/[.035]" /></Field></div><div className="mt-4 flex items-center gap-3 rounded-xl border border-white/[.07] bg-white/[.02] px-4 py-3"><input id="verified" type="checkbox" className="size-4 accent-[#e8e1c5]" /><label htmlFor="verified" className="text-xs text-slate-400">Prioritize verified accounts</label></div></div></section><aside className="space-y-5"><div className="glass-card rounded-2xl p-6"><p className="eyebrow">Campaign builder</p><h2 className="mt-2 font-[Manrope] text-xl font-bold tracking-tight text-white">Create the brief</h2><div className="mt-5 space-y-4"><Field label="Campaign name"><Input required value={name} onChange={(event) => setName(event.target.value)} placeholder="AI founder discovery" className="border-white/10 bg-white/[.035]" /></Field><Field label="Initial message"><Textarea required value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Write the core message your team wants to send…" className="min-h-32 border-white/10 bg-white/[.035]" /></Field><Field label="Daily DM limit" hint="Activity control"><Input type="number" min={1} max={500} value={dailyLimit} onChange={(event) => setDailyLimit(Number(event.target.value))} className="border-white/10 bg-white/[.035]" /></Field></div><Button type="button" onClick={() => generate.mutate({ audience, message })} disabled={generate.isPending || audience.length < 3 || message.length < 10} variant="outline" className="button-press mt-5 w-full border-[#e8e1c5]/20 bg-[#e8e1c5]/[.05] text-[#f5f0df] hover:bg-[#e8e1c5]/[.1] hover:text-[#f5f0df]">{generate.isPending ? <Loader2 className="mr-2 size-4 animate-spin" /> : <Sparkles className="mr-2 size-4" />}Generate AI variations</Button>{variations.length > 0 && <div className="mt-4 space-y-2">{variations.map((variation, index) => <div key={variation} className="rounded-xl border border-white/[.07] bg-white/[.025] p-3"><span className="mono text-[9px] text-[#e8e1c5]">VARIATION 0{index + 1}</span><p className="mt-1.5 text-xs leading-5 text-slate-400">{variation}</p></div>)}</div>}<Button type="submit" disabled={create.isPending} className="button-press mt-5 w-full rounded-xl bg-[#e8e1c5] text-[#20362b] hover:bg-[#f2ecd6]">{create.isPending ? <Loader2 className="mr-2 size-4 animate-spin" /> : <Play className="mr-2 size-4" />}Save as draft</Button><p className="mt-3 text-center text-[10px] leading-4 text-slate-500">Review and schedule campaigns before any outreach starts.</p></div></aside></form></div>;
}

function Field({ label, hint, children }: { label: string; hint?: string; children: React.ReactNode }) { return <div><div className="mb-2 flex items-center justify-between"><Label className="text-xs font-medium text-slate-300">{label}</Label>{hint && <span className="text-[10px] text-slate-600">{hint}</span>}</div>{children}</div>; }
